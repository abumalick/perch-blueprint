node_modules/
dist/
*.log
.DS_Store
.tmp/
test-results/
playwright-report/
deploy/agent.env
deploy/*.log

# Git worktrees for feature work (see docs/WORKTREES.md)
.worktrees/

# Live-serve symlinks: pwa-live (serve-build.sh) points at the PWA build being served;
# agent-live (serve-agent.sh) points at the checkout the agent runs from.
deploy/pwa-live
deploy/agent-live

# Machine-local agent notes (per-computer, not shared)
CLAUDE.local.md

# Local design notes (brainstorm specs & plans) — kept out of the repo
docs/superpowers/
